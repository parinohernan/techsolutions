---
import { Image } from 'astro:assets';
import techSolutionsLogo from '../assets/logo.png';
import { motion } from "framer-motion";
---

<section class="hero">
    <div class="container">
      <motion.div 
        client:load
        className="logo-container"
        initial={{ x: "-100%", scale: 4, opacity: 0.5 }}
        animate={{ x: 0, scale: 1, opacity: 1 }}
        transition={{ duration: 2, ease: "easeOut" }}
      >
        <Image 
          src={techSolutionsLogo} 
          alt="TechSolutions Logo" 
          width={180} 
          height={180}
          class="logo"
        />
        <!-- <span class="hero-title">TechSolutions</span> -->
        <span style="color: #000000; background-color: #ffffff;">T</span>
        <span style="color: #000000; background-color: #ffffff;">e</span>
        <span style="color: #000000; background-color: #ffffff;">c</span>
        <span style="color: #000000; background-color: #ffffff;">h</span>
        <span style="color: #2486d6; background-color: #ffffff;">s</span>
        <span style="color: #000000; background-color: #ffffff;">o</span>
        <span style="color: #000000; background-color: #ffffff;">l</span>
        <span style="color: #000000; background-color: #ffffff;">u</span>
        <span style="color: #000000; background-color: #ffffff;">t</span>
        <span style="color: #2486d6; background-color: #ffffff;">i</span>
        <span style="color: #000000; background-color: #ffffff;">o</span>
        <span style="color: #000000; background-color: #ffffff;">n</span>
        <span style="color: #2486d6; background-color: #ffffff;">s</span>
      </motion.div>
      <h1>Soluciones Tecnológicas para PYMES</h1>
      <p>
        Optimiza tus procesos y mejora la eficiencia de tu empresa con nuestras 
        soluciones personalizadas de software y soporte técnico.
      </p>
      <a href="#contacto" class="cta-button">Contáctanos</a>
    </div>
</section>

<style>
    .hero {
      padding: 8rem 2rem;
      text-align: center;
      margin-top: 4rem;
      overflow-x: hidden;
    }
  
    .container {
      max-width: 800px;
      margin: 0 auto;
    }

    .logo-container {
      margin-bottom: 2rem;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 1rem;
      font-size: 1.8rem;
      font-weight: 700;
      color: var(--primary-color);
    }

    .logo {
      width: 180px;
      height: 180px;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .hero-title {
      font-size: 1.8rem;
      font-weight: 700;
      color: var(--primary-color);
    }
  
    h1 {
      font-size: 3rem;
      margin-bottom: 1.5rem;
      color: var(--text-color);
    }
  
    p {
      font-size: 1.25rem;
      margin-bottom: 2rem;
      color: var(--text-color);
    }
  
    .cta-button {
      display: inline-block;
      padding: 1rem 2rem;
      background: var(--primary-color);
      color: white;
      text-decoration: none;
      border-radius: 0.5rem;
      font-weight: 500;
      transition: background 0.3s ease;
    }
  
    .cta-button:hover {
      background: var(--secondary-color);
    }

    @media (max-width: 768px) {
      .logo {
        width: 150px;
        height: 50px;
      }

      .hero-title {
        font-size: 1.5rem;
      }

      h1 {
        font-size: 2rem;
      }
    }
</style>